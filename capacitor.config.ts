import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.qrgenerator.hiterharris',
  appName: 'qr-generator',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
